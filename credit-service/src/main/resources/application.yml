server:
  port: 8090
  
spring:
  application:
    name: credit-service
          
eureka:
  instance:
    hostname: localhost
    metadata-map:
      version: current
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka